{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" } },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(rate(insightiq_http_requests_total[5m]))",
          "legendFormat": "rps"
        }
      ],
      "title": "Gateway request rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" } },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(insightiq_http_request_duration_seconds_bucket[5m])) by (le))",
          "legendFormat": "p95"
        }
      ],
      "title": "Gateway latency p95",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["insightiq"],
  "time": { "from": "now-1h", "to": "now" },
  "title": "InsightIQ Gateway",
  "uid": "insightiq-gateway",
  "version": 1
}
