openapi: 3.1.0
info:
  title: InsightIQ API
  version: 0.0.0
servers:
  - url: http://localhost:8000
paths:
  /healthz:
    get:
      operationId: healthz
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string }
                required: [status]
