## InsightIQ Frontend (Angular scaffold)

Phase 0 provides a minimal Angular app shell.

### Run (local)

```bash
cd insightiq/frontend
npm install
npm start
```

The app expects the gateway at `http://localhost:8000`.
